# 2026-02-21

Saturday morning, steady pace, useful refinements.

## Digestion protocol tuning

Started at home during **Stomach time (09:29)** with a clear question: why is digestion feeling strained? The conversation stayed grounded — not "find the magic supplement," more "which part of the load is hardest to process?"

The likely culprit looks like **morning density**: big protein, stacked supplements, compressed window. So we shifted from brute force to sequencing:

- Split smoothie into two servings
- Spread supplements across morning / midday / evening
- Add only one fiber lever at a time (chia or ground flax)

By **Spleen time (10:14, home)** it felt coherent. Less hacking, more protocol design.

## Notes architecture cleanup

Then we restructured the Obsidian system itself:

- **Daily Eating Structure** = composition + baseline protocol (clock-agnostic)
- **Meridian Clock (Solar-Adjusted)** = timing + lifestyle architecture

That split instantly reduced conceptual clutter. Food structure in one place, temporal rhythm in another.

We also expanded the meridian note with a full 12-meridian start/end snapshot and energy blurbs, so it reads less like theory and more like a practical day map.

And one strong behavioral commitment landed: **no screens after Kidney meridian starts**. Digital sunset as a real rule, not a vague intention.

## Breadcrumbs begin

Location shifted at **11:23 — Son of Tucci — Spleen**. That mattered. Hamlet had been the dominant mid-morning container recently; today introduced a new environmental texture.

We started a breadcrumb system (time + place + meridian) to track how place and energetic phase interact across the day — light-touch tracking, high long-term signal.

## Tone of the day

No fireworks, just quality refinement.

Today felt like tightening bolts: digestion protocol cleaner, notes cleaner, rhythm cleaner. Incremental work, but exactly the kind that compounds.

## Pivot

At 11:35 we intentionally switched topics. Framing for the next sprint: stop polishing prose style for now, and put the next 30 minutes into shipping the Obsidian → website link-up. Clear, bounded, achievable.

## Obsidian → Website sprint (café close)

This turned into a real infrastructure step forward.

We implemented a working publish pipeline in `~/Documents/quartz-site` with:
- `scripts/obsidian-publish.sh`
- `scripts/obsidian-publish.conf`
- `scripts/node22.sh` (pins Node 22 for Quartz reliability)
- npm commands: `sync:obsidian` and `build:obsidian`

We also shifted to a cleaner vault model:
- `Public/` = intended for publishing
- `Private/` = non-published by default

There were two practical blockers and both got cleared:
1. **Build crash** (`esbuild` deadlock/SIGBUS) resolved by pinning build commands to Node 22.
2. **Silent content omission** due to `Public` path handling in Quartz. Fixed by remapping synced output to `content/obsidian/published/...`.

GitHub auth briefly blocked push, then recovered after re-auth. Final commits landed on `v4` and were pushed.

Net result at session close: pipeline is operational, structure is in place, and publication flow is now repeatable. Refinement still needed, but this was a meaningful foundation pass.